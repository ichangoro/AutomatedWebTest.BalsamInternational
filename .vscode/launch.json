{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug Playwright Test",
      // "program": "${workspaceFolder}/node_modules/@playwright/test/cli.js",
      "program": "${workspaceFolder}/run-playwright-debug.js",
      "args": [
        // "test",
        // "specs/tests/${fileBasename}"
        "${file}"
      ],
      "console": "integratedTerminal"
    }
  ]
}